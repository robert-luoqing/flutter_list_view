import 'package:flutter/material.dart';

class MultipleSlivers extends StatefulWidget {
  const MultipleSlivers({super.key});

  @override
  State<MultipleSlivers> createState() => _MultipleSliversState();
}

class _MultipleSliversState extends State<MultipleSlivers> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
